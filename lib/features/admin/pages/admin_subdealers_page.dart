import 'package:flutter/material.dart';

class AdminSubdealersPage extends StatelessWidget {
  const AdminSubdealersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text('TODO')),
    );
  }
}
