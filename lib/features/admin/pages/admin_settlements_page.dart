import 'package:flutter/material.dart';

class AdminSettlementsPage extends StatelessWidget {
  const AdminSettlementsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('TODO')),
    );
  }
}
