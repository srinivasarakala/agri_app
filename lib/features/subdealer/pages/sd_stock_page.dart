import 'package:flutter/material.dart';

class SdStockPage extends StatelessWidget {
  const SdStockPage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text('TODO')),
    );
  }
}
