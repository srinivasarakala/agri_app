import 'package:flutter/material.dart';

class SdLedgerPage extends StatelessWidget {
  const SdLedgerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text('TODO')),
    );
  }
}
