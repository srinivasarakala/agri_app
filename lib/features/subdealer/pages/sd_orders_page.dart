import 'package:flutter/material.dart';

class SdOrdersPage extends StatelessWidget {
  const SdOrdersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text('TODO')),
    );
  }
}
